{
  "name": "{{#if profile}}{{ profile }}-{{/if}}monitoring{{#if stage}}-{{ stage }}{{/if}}",
  "version": "1.0.0",
  "scripts": {
    "synth": "cdk synth",
    "version": "cdk --version",
    "bootstrap": "cdk {{#if profile}}--profile {{ profile }} {{/if}}bootstrap",
    "diff": "cdk {{#if profile}}--profile {{ profile }} {{/if}}diff",
    "deploy": "cdk {{#if profile}}--profile {{ profile }} {{/if}}deploy"
  },
  "devDependencies": {
    "@types/node": "^12.12.54",
    "aws-cdk": "^1.58.0",
    "ts-node": "^8.1.0",
    "typescript": "~3.9.7"
  },
  "dependencies": {
    "mca-monitoring": "https://github.com/nordcloud/mca-monitoring/releases/download/v0.0.5/mca-monitoring-0.0.5.tgz"
  }
}
